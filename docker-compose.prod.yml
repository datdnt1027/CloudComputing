version: '3'
services:
  website:
    container_name: 'website'
    image: '${WEBSITE_IMAGE}'
    ports:
      - '3000:3000'
    volumes:
      - website:/website/build/ # build files will be added at this directory !!!

volumes:
  website: